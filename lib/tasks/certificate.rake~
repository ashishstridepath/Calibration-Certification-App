namespace :certificate do

  task daily_task: :environment do
    @customers = Customer.all
    @customers.each do |customer|
        if customer.email_address 

 
         UserMailer.send_certificate(customer).deliver
 
 
        end
    end
 end
end
