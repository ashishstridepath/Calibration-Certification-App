


<%- model_class = TestWeight -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<!--<div class="row">
  <div class="col-lg-3 pull-right">
      <div class="input-group custom-search-form">
           <%= search_form_for @search do |f| %>
              <div class="field">
                <%= f.label :date_calibrated_cont, "Search Test Weight" %>
                <%= f.text_field :date_calibrated_cont %>
              </div>
              <div class="actions"><%= f.submit "Search", :class => "button" %></div>
            <% end %>            
       </div><!-- /input-group -->
    </div>
</div>           
  

<table class="table ">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:Set_Name) %></th>
      <th><%= model_class.human_attribute_name(:Certificate_Number) %></th>
      <th><%= model_class.human_attribute_name(:Date_Calibrated) %></th>
      
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @test_weights.each do |test_weight| %>
      <tr class="selected-row">  
        <td data-open='<%= test_weight_path(test_weight) %>'><%= test_weight.name %></td>
        <td data-open='<%= test_weight_path(test_weight) %>'><%= test_weight.certificate_number %></td>
        <td data-open='<%= test_weight_path(test_weight) %>'><%= test_weight.date_calibrated %></td>
      
           
        <td>
          <%= link_to 'edit',edit_test_weight_path(test_weight), :class => "btn btn-primary" %>
          
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      test_weight,
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>


            <% link_to "Add", "#new_test_weight_modal", :data => { :toggle=>"modal" }, :class => "btn btn-primary" %>
            
            <%= link_to "Add Test Weight", new_test_weight_path, :class => "btn btn-primary" %>
            
            
            
            <div class="modal fade" id="weight-modal-window">
              <div class="modal-dialog modal-lg">
                <div class="modal-content">
                  <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span></button>
                    <h4>Test_Weight</h4>
                  </div>
                  <div class="modal-body" id="edit_modal_body">
                    
                  </div>
                  <div class="modal-footer">
                    <hr/>
                  </div>
                </div>
              </div>
            </div>

  <div align="center"><%= will_paginate @test_weights, renderer: BootstrapPagination::Rails %>
         </div>
      
