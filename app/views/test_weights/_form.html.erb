<%= form_for(@test_weight, :html => { :class => "form-horizontal" }) do |f| %>
  <% if @test_weight.errors.any? %>
    <div class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <h4><%= pluralize(@test_weight.errors.count, "error") %> prohibited this test_weight from being saved:</h4>

      <ul>
      <% @test_weight.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :Set_Name, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :name, :class => "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :certificate_number, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :certificate_number, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :date_calibrated, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.date_select :date_calibrated, :class => "form-control" %>
    </div>
  </div>
  
  
  <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit :class => "btn btn-primary" %> 
      <%= link_to t('.back', :default => t("helpers.links.back")),
              :back, :class => 'btn btn-primary'  %>
      </div>        
  </div>
<% end %>
